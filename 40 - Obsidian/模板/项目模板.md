---
tags:
  - project/open
  - project/index
cssclasses: wide-page
target: tasks
status: 计划中
<%* 
  const typeOptions = ["论文", "基金", "报告", "书籍", "其他"]; 
  const typeChoice = await tp.system.suggester(typeOptions, typeOptions, false, "选择项目类型"); 
  const type = typeChoice || "其他";
  
  let banner = "40 - Obsidian/img/PaperBell.jpg";
  if (type === "书籍") banner = "40 - Obsidian/img/library.gif";
  else if (type === "报告") banner = "40 - Obsidian/img/讨论.jpg";
  else if (type === "基金") banner = "40 - Obsidian/img/结果.jpg";
  else if (type === "其他") banner = "40 - Obsidian/img/方法.jpg";
  
  tR += `type: ${type}\n`;
  tR += `banner: "${banner}"`;
%>
start: <% tp.date.now('YYYY-MM-DD') %>
end:
workload: <%* const workloadOptions = ["1", "2", "3", "4", "5"]; const workloadChoice = await tp.system.suggester(workloadOptions, workloadOptions, false, "选择工作量 (1-5)"); tR += workloadChoice || "1"; %>
duty: <%* const dutyOptions = ["负责人", "参与者", "打工人"]; const dutyChoice = await tp.system.suggester(dutyOptions, dutyOptions, false, "选择你的角色"); tR += dutyChoice || "负责人"; %>
project: <%* 
  const acronym = await tp.system.prompt("项目缩写 (Acronym) / ID (必填且唯一)");
  const currentFile = tp.file.find_tfile(tp.file.path(true));

  if (!acronym) {
      if (currentFile) { await app.vault.trash(currentFile, true); }
      new Notice("未输入项目ID，已取消创建并删除草稿。");
      throw new Error("未输入项目ID，已取消。");
  }
  
  const fileExists = await tp.file.exists(acronym);
  
  if (fileExists) {
      const targetFile = tp.file.find_tfile(acronym);
      if (targetFile && currentFile && targetFile.path !== currentFile.path) {
          await app.vault.trash(currentFile, true);
          new Notice(`错误：项目 ID "${acronym}" 已存在！已删除当前草稿。`);
          throw new Error(`项目 ID "${acronym}" 已存在，已取消。`);
      }
  }
  
  tR += acronym; 
  
  if (acronym !== tp.file.title) { 
      try {
        await tp.file.rename(acronym); 
      } catch (error) {
        if (currentFile) { await app.vault.trash(currentFile, true); }
        new Notice("重命名失败，已删除草稿：" + error.message);
        throw error;
      }
  } 
%>
long_name: <%* const longName = await tp.system.prompt("项目全称 (Full Name)"); tR += longName || tp.file.title; %>
related: <%* 
  const relatedInput = await tp.system.prompt("关联项目 (逗号分隔)"); 
  if (relatedInput) {
    tR += "\n" + relatedInput.split(/[,，]/).map(item => `  - "[[${item.trim().replace(/["']/g, "")}]]"`).join("\n");
  }
%>
venue:
funding:
collaborators:
url: <%* const url = await tp.system.prompt("项目官网链接 (URL)"); tR += url || ""; %>
---

## 👋 README

介绍:: <%* const intro = await tp.system.prompt('项目简介'); tR += intro; %>

## 🔬 Research Design

### Research Question / Goal
- 

### Methodology
- 

### Data / Materials
- 

## 🙋 Milestones

- [x] 创建项目 <% tp.file.title %>
- [ ] 完善研究设计
- [ ] 你可以在[Task Genius 插件](obsidian://adv-uri?vault=PaperBell&commandid=obsidian-task-progress-bar%3Aswitch-view-projects)集中管理项目待办啦！

## 🧭 Navigation
