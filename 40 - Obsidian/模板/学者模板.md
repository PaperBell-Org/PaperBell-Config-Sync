---
name: <% tp.file.title %>
<%* 
  const aliasesInput = await tp.system.prompt("请输入别名（多个别名用逗号分隔，留空跳过）", "");
  if (aliasesInput && aliasesInput.trim()) {
    const aliases = aliasesInput.split(/[,，]/).map(a => a.trim()).filter(a => a);
    if (aliases.length > 0) {
      tR += "aliases:\n";
      aliases.forEach(alias => {
        tR += `  - ${alias}\n`;
      });
    }
  }
  const gender = await tp.system.prompt("性别", "");
  if (gender) {
    tR += `gender: ${gender}\n`;
  }
  const birthday = await tp.system.prompt("生日", "");
  if (birthday) {
    tR += `birthday: ${birthday}\n`;
  }
  const email = await tp.system.prompt("邮箱", "");
  if (email) {
    tR += `email:\n  - ${email}\n`;
  }
  const title = await tp.system.prompt("称谓", "");
  if (title) {
    tR += `title:\n  - ${title}\n`;
  }
  const website = await tp.system.prompt("主页链接", "");
  if (website) {
    tR += `website: ${website}\n`;
  }
  const photo = await tp.system.prompt("照片链接", "");
  if (photo) {
    tR += `photo: "${photo}"\n`;
  }
  tR += "tags: scholar\n";
  const institute = await tp.system.prompt("所在机构", "");
  if (institute) {
    tR += `institute:\n  - ${institute}\n`;
  }
  tR += `following_date: ${tp.date.now('YYYY-MM-DD')}\n`;
%>
---

## 基本信息

````ad-flex

<div>

<br>

🧑‍🔬`=(this.name)`
📮`=(this.email)`
🏫`=link(this.institute)`
🔗`=(this.website)`

</div>


<div>


`=("![](" + this.photo + ")")`


</div>

````

## 研究兴趣

-

## 相关论文

![[论文检索.base]]

## 最新动态

```dataviewjs

let folderChoicePath = "00 - 每日日记/DailyNote"
const files = app.vault.getMarkdownFiles().filter(file => file.path.includes(folderChoicePath))
let names = dv.current().aliases ? dv.current().aliases : [];
names.push(dv.current().name)


let arr = files.map(async(file) => {
    const content = await app.vault.cachedRead(file)
    let lines = await content.split("\n").filter(line => names.some(name => line.includes(name)))
    //console.log(lines)
    return ["[["+file.name.split(".")[0]+"]]", lines]
})

Promise.all(arr).then(values => {
    const beautify = values.map(value => {
        const temp = value[1].map(line => { return line }) //美化要重写
        return [value[0],temp]
    })
    const exists = beautify.filter(value => value[1][0] && value[0] != "[[未命名 10]]") .sort(value => value[0],'desc')
    dv.table(["日期", "动态"], exists)
})
```
